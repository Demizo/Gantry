[project]
name = "zds"
version = "0.1.0"
description = "Python environment for ZDS"
authors = [
    { name = "Demizo", email = "demizodemazo@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "jinja2>=3.1.6",
]

[tool.uv]
package = false

[build-system]
requires = ["uv_build>=0.10.6,<0.11.0"]
build-backend = "uv_build"
